let counter = (document.querySelector("input-field").innerHTML = 4);
console.log("muhammed is learning version control system");

let apiKey = {
  name: olaidetnde,
  username: baba,
};

if (apiKey.includes("name")) {
  `uou are eligible`;
}
